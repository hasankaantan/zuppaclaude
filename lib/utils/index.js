/**
 * Utils index
 */

const { Logger, colors } = require('./logger');
const { Platform } = require('./platform');
const { Prompts } = require('./prompts');

module.exports = {
  Logger,
  colors,
  Platform,
  Prompts,
};
